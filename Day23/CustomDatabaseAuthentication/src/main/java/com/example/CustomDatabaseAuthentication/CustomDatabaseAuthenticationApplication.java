package com.example.CustomDatabaseAuthentication;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * The type Custom database authentication application.
 */
@SpringBootApplication
public class CustomDatabaseAuthenticationApplication {

	/**
	 * The entry point of application.
	 *
	 * @param args the input arguments
	 */
	public static void main(String[] args) {
		SpringApplication.run(CustomDatabaseAuthenticationApplication.class, args);
	}

}
